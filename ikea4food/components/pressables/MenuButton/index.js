import React from 'react';
import { 
    View,
} from 'react-native';
import {
    Icon,
} from 'react-native-elements';

export default MenuButton = () => {
    return (
        <View>
            <Icon
            name='menu'
            type='entypo'
            color='#fff'/>
        </View>
    );
}